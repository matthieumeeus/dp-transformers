defaults:
  - workspace: M365Research-PPML-EUS
  - _self_

shared_training_parameters:
  num_train_epochs: 1.0
  learning_rate: 0.00031317026507451934
  base_model: azureml:gpt2:1
  sequence_len: 1024
  lr_scheduler_type: constant
  per_device_train_batch_size: 8
  gradient_accumulation_steps: 2

shared_inference_parameters:
  per_device_batch_size: 32

game_config:
  num_models: 1
  seed: 1920
  num_concurrent_jobs_per_node: 1
  num_challenge_points_per_model: 1024

shadow_model_config:
  in_fraction: 0.5
  num_models: 8

mi_signal_config:
  method: CrossEntropy
  aggregation: expsum
  extra_args:
    temp: 2.0 

rmia_config: {}

canary_config:
  method: generate_secrets